services:
  - type: web
    name: autonomous-rag-system
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: LIGHTWEIGHT_DEPLOY
        value: "1"
      - key: OPENAI_API_KEY
        sync: false
